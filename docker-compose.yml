services:
  nkust-auto-class:
    container_name: nkust-auto-class
    image: jianzhe61/nkust-auto-class:latest
    ports:
      - "5000:5000"
    volumes:
      - nkust_data:/data
    shm_size: '2gb'
    restart: unless-stopped
    environment:
      - TZ=Asia/Taipei

volumes:
  nkust_data:
